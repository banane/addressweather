<!DOCTYPE html>
<html>
  <head>
    <title>Address Weather</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>


    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style type="text/css">
    body { 
      margin-left: 30px;
      font-family: sans-serif;
    }
    label {
      display: block;
      padding-top: 20px;
      padding-bottom: 10px;
    }
    h1 { 
      color: "#cc00ff";
    }   
    
    input#submitButton  {
      margin-block: 30px;
      display: block;
      font-weight: bold;
      backgroundColor: "darkGrey";
      color: "#fff";
      width: 200px;
      height: 40px;
    }
    </style>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
